defaults:
  - data: task_qc_baseline
  - model: qc_baseline
  - paths: default
  - trainer: qc_trainer
  - logger: wandb
  - callbacks: task_qc_baseline
  - override hydra/sweeper: sweeper
  - override hydra/launcher: joblib
  - _self_
  
## job specific set ups ##
exp_name: ISBI_qc_sweep
trainer:
  min_epochs: 0 # prevents early stopping
  max_steps: 25000
job_name: 
logger:
  wandb:
    name: 
    project: ${exp_name}
hydra:
  launcher:
    n_jobs: 1   # parallel processes
device: "cuda:0"

eval_only: False 
debug: False 

resume: False
reset_epoch: False 
resume_optim: True
resume_lr_scheduler: True
freeze_feat: False
load_backbone: False
feat_ckpt:
resume_training: False
resume_ckpt: 
## paths 
# out_dir: ISBI/${exp_name}/${job_name}



n_epochs: 400



seed: 6546




